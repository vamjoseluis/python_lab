- Install pyenv

```
$ brew install pyenv
```

- Add pyenv initializer to shell
```
$ echo 'eval "$(pyenv init --path)"' >> ~/.bash_profile
```

- Reload the bash_profile
```
source ~/.bash_profile
```




